apiVersion: v1
kind: Secret
metadata:
  name: zitadel-secrets
type: Opaque
stringData:
  config.yaml: |
    Database:
      Postgres:
        User:
          Username: zitadel
          SSL:
            Mode: verify-full
        Admin:
          Username: postgres
          SSL:
            Mode: verify-full
