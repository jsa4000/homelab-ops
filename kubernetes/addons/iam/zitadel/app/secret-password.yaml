apiVersion: v1
kind: Secret
metadata:
  name: zitadel-secrets
type: Opaque
stringData:
  config.yaml: |
    Database:
      Postgres:
        User:
          Username: app
          Password: password
          SSL:
            Mode: disable # verify-full
        Admin:
          Username: postgres
          Password: password
          SSL:
            Mode: disable # verify-full
