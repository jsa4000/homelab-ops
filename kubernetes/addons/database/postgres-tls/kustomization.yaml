apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: database

resources:
  #  - password-cluster
  - cluster
  - backup
  # Don't needed since it's already created with the cluster
  #  - monitoring
  - dashboards
