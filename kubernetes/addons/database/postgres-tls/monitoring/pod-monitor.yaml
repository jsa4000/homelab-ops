apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres-tls-cluster
spec:
  selector:
    matchLabels:
      postgresql: postgres-tls-cluster
  podMetricsEndpoints:
    - port: metrics
