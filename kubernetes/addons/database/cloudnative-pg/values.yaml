replicaCount: 1

crds:
  create: true

config:
  create: true

monitoring:
  podMonitorEnabled: false
  grafanaDashboard:
    create: true
    annotations:
      grafana_folder: "Database"
    labels:
      grafana_dashboard: "true"
