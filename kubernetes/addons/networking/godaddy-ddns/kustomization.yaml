apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: networking

resources:
  - namespace.yaml
  - deployment.yaml
  - external-secret.yaml

configMapGenerator:
  - name: godaddy-ddns
    literals:
      - GD_NAME=@
      - GD_DOMAIN=javstudio.org
      - GD_TTL=600
