apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: emqx-dashboard
    files:
      - dashboard.json
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    grafana_folder: "Home"
  labels:
    grafana_dashboard: "true"
