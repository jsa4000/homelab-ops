apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/jsa4000/homelab-ops//kubernetes/addons/iam/zitadel?ref=main

patches:
  - path: ingress-patch.yaml

# Check if there is additional configMaps to add to the configuration
configMapGenerator:
  - name: zitadel-config
    files:
      - zitadel-config.yaml
